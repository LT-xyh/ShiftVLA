# E2.3 — Canonical authority graph reconciliation

## Verdict

**E2 remains UNRESOLVED.** The E2.1/E2.2 contradiction is resolved by a
machine-derived graph generated directly from `inspect()`. This report is a
rendering of `G1-E2.3-authority-graph.json`; the regression suite checks the
fixed offsets and callable identities against that JSON-producing path.

## Canonical REDUCE table

| offset | callable symbolic ID / authority | argument symbolic ID | result ID |
|---:|---|---|---|
| 104 | `GLOBAL#1` = `_codecs encode` | `TUPLE#2` | `REDUCE_RESULT#5` |
| 110 | `GLOBAL#6` = `numpy.core.multiarray _reconstruct` | `TUPLE#7` | `REDUCE_RESULT#11` |
| 152 | `GLOBAL#12` = `numpy dtype` | `TUPLE#13` | `REDUCE_RESULT#17` |
| 43851 | `GLOBAL#1` = `_codecs encode` | `TUPLE#18` | `REDUCE_RESULT#20` |

The previous E2.1/E2.2 disagreement is therefore resolved: REDUCE@104 and
REDUCE@43851 call `_codecs.encode`; REDUCE@110 calls
`numpy.core.multiarray._reconstruct`; REDUCE@152 calls `numpy dtype`.

## Canonical BUILD table

| offset | target symbolic ID | state symbolic ID |
|---:|---|---|
| 184 | `REDUCE_RESULT#17` | `TUPLE#21` |
| 43857 | `REDUCE_RESULT#11` | `TUPLE#30` |

BUILD mutates the exact symbolic target; it does not create a replacement ID.
The graph preserves argument/state trees as references to structural symbols,
with all raw literal leaves classified `PHYSICAL_OPAQUE` by default.

## Structural versus physical values

Retained structural metadata consists only of opcode position, tuple/list/dict
shape, memo identity, and exact GLOBAL authority strings. Raw BINUNICODE,
BINBYTES, integers, booleans, and None leaves are not retained as payload
values by `inspect()`; they are represented by kind plus an opaque marker.
No physical simulator bytes, state values, or array contents occur in the JSON
graph. No literal is promoted merely because it is short or printable.

## Predicate status

- exact ndarray subtype argument: **CLOSED structurally** — the reconstruct
  argument tree reaches the direct `GLOBAL numpy ndarray` symbol;
- dynamic callable/type substitution: **CLOSED for fixed bytes** — all four
  reducer callables are direct GLOBAL nodes, with no STACK_GLOBAL/EXT/NEWOBJ or
  indirect GLOBAL;
- BUILD@184 target: **CLOSED structurally** as `REDUCE_RESULT#17`;
- BUILD@43857 target: **CLOSED structurally** as `REDUCE_RESULT#11`;
- BUILD state structural schema: **STILL_UNRESOLVED** beyond opaque tree shape;
- dtype descriptor structural value: **STILL_UNRESOLVED** because raw leaves
  remain opaque by policy;
- validation/use consistency: **CROSS_PACKAGE_DEPENDENCY_ON_FUTURE_ADMISSION**.

No NumPy/Torch semantic or native conclusion is made here. E3 remains
unstarted.

## Evidence and checks

Fixed input was independently checked with stdlib `zipfile`,
`pickletools.genops`, and SHA-256. Synthetic tests cover scalar/container
stack effects, memo identity, malformed references, chained reducer identity,
BUILD identity, unsupported authority opcodes, and recursive secret-marker
absence from the returned `inspect()` graph. The fixed asset was scanned only
after those tests passed.

```text
PYTHONPATH=. ... pytest -q tests/test_m1_e2_symbolic.py  # 9 passed
PYTHONPATH=. ... scripts/m1_e2_authority_graph.py
PYTHONPATH=. ... scripts/m1_e2_symbolic.py                 # sanitized output only
git diff --check
```

No pickle execution, reducer/BUILD/dtype/codec execution, Torch/NumPy/LIBERO/
MuJoCo/official-runtime import, environment/EGL/render, E3–E6, or G2 occurred.
