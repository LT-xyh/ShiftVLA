# Prefix reexecution microvalidation authorization

**Status: RUNTIME MICROVALIDATION AUTHORIZED EXACTLY ONCE.**

Branch: `xyh/replayvla-p1`

Reviewed implementation commit:

`3669a1b7c2bd05438b40351bc4cf7e066286a6f8`

Scientific route authority:

`docs/replayvla-p1/16_prefix_reexecution_paper_route.md`

This authorization is only for a bounded non-scientific qualification run.

It does not authorize the 40-rollout pilot.

## 1. Validation sequence

Exactly one harness invocation is authorized.

The sequence is fixed:

1. focused pytest;
2. MV-P2-A same-current-state camera intervention;
3. MV-P2-B real switch-index lifecycle;
4. MV-P1 one-query DCU explicit-noise select_action;
5. compact evidence publication;
6. STOP.

Later stages run only if every earlier stage PASSes.

## 2. Non-scientific scope

The run is a MICROVALIDATION.

It must retain:

- scientific_rollout = false;
- pilot_rollout_count = 0;
- paper_estimands_computed = false;
- retry = 0;
- replacement = 0.

It must not compute G_C, G_S, L, I or any paper result.

## 3. Focused tests

The harness must first run:

`tests/test_p1_prefix_reexecution.py`

and

`tests/test_p1_prefix_reexecution_microvalidate.py`.

If focused pytest does not PASS, no LIBERO runtime or DCU worker may start.

## 4. MV-P2-A

Frozen root:

- suite: libero_spatial;
- task: 0;
- init state: 0;
- seed: 2027.

MV-P2-A qualifies the observation-only camera mutation at one current physical state.

Required PASS evidence includes:

- agentview shape exactly 360x360x3 uint8;
- clean image SHA differs from shifted image SHA;
- restored-clean image SHA equals original clean image SHA;
- clean -> shifted and clean -> restored intervals preserve qpos/qvel/ctrl exactly where defined;
- sim time remains exact;
- camera position remains exact;
- camera FOV remains exact;
- clean cam_quat is exactly restored;
- no policy/model query;
- no policy-indexed env step.

If any condition fails, stop before MV-P2-B.

## 5. MV-P2-B

Use a fresh CPU environment runtime.

Frozen branch:

- arm: CS;
- switch index: 50;
- same task/init/seed as above.

Use the official LeRobot LIBERO dummy action.

Execute exactly 50 calls through `ObservationIndexedCameraWrapper.step`.

Required lifecycle:

- obs_0..obs_49 request clean camera;
- action index 49 requests shifted camera for obs_50;
- final camera evidence:
  - observation_index = 50;
  - preceding_action_index = 49;
  - requested_camera_mode = shifted;
- wrapper action_index = 50;
- reset count = 1;
- wrapper step calls = 50;
- policy query count = 0;
- model query count = 0.

If the environment terminates/truncates before completion, MV-P2-B is BLOCKED and MV-P1 must not start.

## 6. MV-P1

Run only after focused pytest, MV-P2-A, and MV-P2-B PASS.

Use the existing ordinary runtime path and frozen compare physical device 1.

Build the CPU processor/runtime with `include_policy=False`.

Generate one real official feature batch from task0/init0/seed2027.

Generate paired flow noise for root:

`libero_spatial-task000-init000-seed2027`

at observation/action index 0.

Execute exactly one real model action query through:

`FeatureOnlyRemotePolicy.select_action(features, noise=noise)`

which must reach the installed worker's official:

`SmolVLAPolicy.select_action(batch, noise=noise)`.

Required queue evidence:

- queue_length_before = 0;
- queue_length_after = 0;
- new_chunk_generated = true;
- n_action_steps = 1;
- explicit noise present in worker inputs.

Official postprocessors may be applied to the returned action for contract validation.

Do not call env.step.

## 7. Evidence

Compact evidence path:

`runtime/replayvla-p1/prefix_reexecution_microvalidation.json`

This path does not exist in the reviewed repository state.

It is immutable and no-overwrite.

Recommended transient work directory:

`runs/replayvla-p1/prefix_reexecution_microvalidation.work`

Do not commit raw IPC tensors or transient work files.

Commit only the compact evidence file generated by the harness.

## 8. Stop rules

Before the harness actually starts, ordinary shell/Git/cwd invocation mistakes may be corrected.

Once the harness has created terminal compact evidence:

- do not rerun it;
- do not delete/rename/overwrite the evidence;
- do not repair implementation/config/runtime and retry;
- do not create a second microvalidation cohort.

If the result is BLOCKED, commit/push the BLOCKED compact evidence and return to reviewer.

If the result is PASS, commit/push the PASS compact evidence and return to reviewer.

## 9. Forbidden

Do not:

- modify implementation or tests during runtime validation;
- install/change packages;
- change runtime identity;
- repair F3N/F3b;
- run exact-state work;
- run the 40-rollout pilot;
- compute paper estimands;
- add tasks/roots/camera severities;
- retry or replacement;
- use git reset --hard;
- use git clean;
- use git add -A.

## 10. Next-phase state

Even if this microvalidation PASSes:

- the 40-rollout pilot remains NOT AUTHORIZED;
- full study remains NOT AUTHORIZED;
- reviewer adjudication is required first.
